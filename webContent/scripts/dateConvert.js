(function(){
  /**
   * parse standard string to human readable date format.
   * 
   * @param {string} date date string getting from server
   * @returns {string}
   */
  function parseToReadableDate (date) {
    const localdate = new Date()
  }

})()